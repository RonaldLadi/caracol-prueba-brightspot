<nav class="main-navigation">
  <button class="nav-toggle" aria-label="Abrir menú">
    ☰
  </button>

  <ul class="nav-list">
    {{#each navigation.[0].items}}
      <li class="nav-item {{#if isCurrent}}is-active{{/if}}">
        <a href="{{href}}">
          {{text}}
        </a>
      </li>
    {{/each}}
  </ul>
</nav>
